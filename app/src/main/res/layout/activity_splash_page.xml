<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:fitsSystemWindows="true"
    android:id="@+id/activity_splash_page"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/splash_wall_paper"
    tools:context="com.leoliao.everydayweather.activity.SplashPage"
    >

    <ImageView
        android:fitsSystemWindows="true"
        android:id="@+id/splash_logo"
        android:layout_width="@dimen/logo_width"
        android:layout_height="@dimen/logo_width"
        android:src="@mipmap/logo"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="@dimen/logo_margin_top"
        />

    <TextView
        android:id="@+id/splash_tv_app_name"
        android:layout_marginTop="@dimen/margin_medium"
        android:layout_below="@id/splash_logo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textColor="@color/colorWhite"
        android:textSize="@dimen/text_size_tool_bar_title"
        android:text="@string/app_name"/>

    <TextView
        android:layout_alignParentBottom="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textColor="@color/colorWhite"
        android:text="Ver.1.0"/>
</RelativeLayout>
